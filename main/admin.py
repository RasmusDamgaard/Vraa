"""
Administration configuration for the ``main`` app.

Currently no models are registered here, but this file exists to
facilitate future expansions where administration interfaces are
required.
"""
from __future__ import annotations

from django.contrib import admin  # type: ignore

# Register your models here when they are defined in ``models.py``.
